import 'package:flutter/material.dart';

class ReCheckPage extends StatelessWidget {
  const ReCheckPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.amber,
      ),
    );
  }
}

class ReCheckPage2 extends StatelessWidget {
  const ReCheckPage2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
